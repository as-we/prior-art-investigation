---
description: |
  Prior Art Investigation (SpecKit) — So-What check triggered before speckit.tasks.
  Verifies tasks reflect prior art findings using Q7 (~2 min).
  Auto-triggered by before_tasks hook in speckit/extension.yml.
tools:
  - runCommand
  - codebase
  - findFiles
---

Run the prior art investigation command in **sowhat** mode.

See `speckit/commands/prior-art.md` for the full workflow.

Arguments passed to command: `sowhat`
